import React from 'react';
import styles from './style.module.less';

export const NotFound: React.FC = (props) => {
  return <div className={`wrapper unselectable ${styles.content}`}>NotFound</div>;
};
