import { RootState } from 'src/reducers';
import { store } from 'src/store';

/*export const isFetchingData = (reducer: StateKeys) => {
    return (store.getState()[reducer] as Fetchable).isFetching;
  };*/
