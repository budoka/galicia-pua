import { RootState } from 'src/reducers';
import { store } from 'src/store';

/*export const isRunningData = (reducer: StateKeys) => {
    return (store.getState()[reducer] as Fetchable).isRunning;
  };*/
